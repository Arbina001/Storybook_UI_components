// TypographyComponent.js

import React from 'react';
import { StyledTextInput } from './Typography.styled.jsjs';

const TypographyComponent = ({ as, text, type }) => {
  return (
    <StyledTextInput as={as} type={type}>
      {text}
    </StyledTextInput>
  );
};

export default TypographyComponent;
